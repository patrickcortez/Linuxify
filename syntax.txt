# this is a Comment

set `""` = yellow; #set color of a special character but they must encase it with ``
set `{}` = blue;

Section <Language Name>{
Keyword: <word>,<color>;
Preprocessor: <word>,<color>;
}

# Example:
# basically nano will read the contents inside the [] to determine what language it is
# they must put the file extension of the language so nano will know, that way its modular and extendable

Section [.cpp]{

    keyword: cout,yellow;
    keyword: int, blue;
    preprocessor: cerr,white;
}


section [.py]{
    keyword: print


}